using Lemoncode.Books.WebApi.Controllers;
using System.Threading.Tasks;
using Xunit;

namespace Lemoncode.Books.FunctionalTests.AuthorControllerTests
{
    public class GetAuthorTests
    {          

        /*
        [Fact]
        public async Task Get_authr_success()
        {
            //Arrange
            int authorId = 1;
            //var fakeOrder = GetFakeOrder();            

            //Act
            var authorsController = new AuthorsController(
                _authorServiceMock.Object,
                _basketServiceMock.Object,
                _identityParserMock.Object);

            orderController.ControllerContext.HttpContext = _contextMock.Object;
            var actionResult = await orderController.Detail(fakeOrderId);

            //Assert
            var viewResult = Assert.IsType<ViewResult>(actionResult);
            Assert.IsAssignableFrom<Order>(viewResult.ViewData.Model);
        }
        */

    }
}
